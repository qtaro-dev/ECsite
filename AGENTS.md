# 自作PCパーツECサイト 実装担当AI向けルール

このファイルは、LUNAを含む実装担当AIが**承認された1チケットずつ**作業するときのガードレールである。現段階では製品実装を始めない。本ファイルの追加だけで実装開始の許可とはみなさない。

## 1. 正本と判断順位

1. ユーザーの最新の明示指示。
2. `output/pdf/自作PCパーツECサイト_要件定義書_レビュー版.pdf` と、それ以後にユーザーが明示承認した送料・個人情報の決定（`docs/detailed-design-review.md`に反映済み）。
3. `output/pdf/自作PCパーツECサイト_基本設計書_レビュー版.pdf`。
4. `output/pdf/自作PCパーツECサイト_詳細設計書_レビュー版.pdf`、編集用の`docs/detailed-design-review.md`。
5. `docs/implementation-plan.md`、2エージェント運用時の`docs/multi-agent-execution-plan.md`、索引`docs/implementation-tickets.md`、現在割り当てられた`tickets/Txx-*.md`、`docs/environment-setup.md`。

上位資料と下位資料が矛盾する、詳細設計に複数案が残る、外部サービスの制約で設計どおりにできない場合は、推測で実装しない。根拠の箇所、影響、選択肢を報告し、仕様変更が必要なら利用者の判断を待つ。通常のライブラリ内部の選択は、設計の範囲内で理由を記録して決めてよい。

## 2. チケット作業の約束

- 1回の作業で1チケットを完了する。目的、範囲、依存、受け入れ条件、必要テストを先に読む。依存チケットの成果を確認してから着手する。
- チケット外の機能、UI、DB、RLS、設定を勝手に変更しない。不可避の小さな関連変更は理由と影響を明記する。追加仕様や設計変更は別チケットとして提案する。
- 作業前に対象ファイルと既存変更を確認する。既存の利用者変更を上書きしない。ソース、マイグレーション、テスト、設計の差分をレビュー可能な単位にする。
- 完了報告に「変更点、設計根拠、実行したテストと結果、未解決の問題、外部設定の要否」を記載する。テストが実行できなければ理由と代替検証を記載し、合格扱いにしない。
- 受け入れ条件を満たせないときは完了扱いにしない。テストを通すために要件・設計・テストを弱めたり、異常系を削除したりしない。

### LUNA-A / LUNA-B の並行作業

- 2エージェントで作業する場合は、着手前に`docs/multi-agent-execution-plan.md`の担当表、Wave、並行条件、競合注意を確認する。担当外チケットや依存未完了チケットに着手しない。
- 別worktree・別チケットブランチを使い、同じファイル、DBマイグレーション、共用テストDBを並行変更しない。競合や設計矛盾は推測で解決せず報告する。相手の変更に依存する場合は、そのPRと必要なDB変更がマージ・適用されるまで待つ。
- 各チケット完了時に変更点、テスト結果、PR/commit、未解決事項を報告する。マージ後の`main`を次チケットの基準にする。
- `docs/implementation-status.html`は47件の進捗一覧。LUNA-Bが各Waveの実装PRマージ後に一覧のみのPRで状態・根拠・更新日を記入する。LUNA-Aは更新に必要な証拠をLUNA-Bへ報告し、一覧を同時編集しない。完了は受け入れ条件、テスト、レビュー、マージの確認後に限る。

## 3. 固定した技術と責務

Next.js App Router、React、TypeScript、CSS Modules、Zod、Supabase Postgres/Auth/Storage/Cron、`@supabase/ssr`、Stripe Checkout**テスト環境**、Vitest、Playwright、GitHub Actions、Vercel Git連携を使う。ライブ決済を導入しない。外部商品API、ゲスト注文、利用者向け取り置き、構成保存は初期版に加えない。

予定ディレクトリ（初期化チケットで作成）：

| パス | 責務 |
| --- | --- |
| `src/app/(shop)/` | 公開ECと会員画面のルート・画面 |
| `src/app/admin/` | 管理画面。画面の非表示だけで権限を守らない |
| `src/app/api/` | Route Handler、Webhook、Auth Hook、内部ジョブ入口 |
| `src/components/`、`src/styles/` | 再利用UI、CSS Modules、デザイントークン |
| `src/features/` | 商品、互換性、カート、注文、決済等の機能別UI・操作 |
| `src/server/` | サーバー専用データ取得・業務処理・外部サービスアダプタ |
| `src/lib/` | 共有型、Zodスキーマ、金額・状態の純粋関数。秘密に依存しない |
| `supabase/migrations/`、`supabase/seed.sql` | 版管理されたSQLと開発・テスト専用データ |
| `tests/`、`docs/api/`、`docs/wireframes/` | Vitest/Playwright/RLS等、OpenAPI、画面ワイヤー |

Server Componentは表示用取得をデータ層から行う。価格・送料・在庫・権限・決済結果はブラウザで確定しない。変更操作は認証・検証したサーバー経路に集め、在庫・注文はDB内の原子的処理で更新する。ドメイン計算を画面とAPIで二重実装しない。

## 4. DB・RLS・認証・秘密

- 全公開スキーマの表でRLSを有効化し、`anon`と`authenticated`には必要最小限の権限のみ付与する。所有者条件を表だけでなく関連明細にも適用する。下書き商品、他人の住所・注文、管理データを公開しない。
- 管理権限は`admin_memberships`で判定し、一般会員が自己付与できない。画面とAPIとDBで検証する。サービスロール鍵は信頼できるサーバー専用で、ブラウザ・`NEXT_PUBLIC_*`・ログ・テスト出力に出さない。
- `auth.users`はSupabase Authが管理する。メール確認、Google OAuth、模擬SMS、パスワード再設定、開発・テスト限定バイパスの条件を設計どおりに守る。Productionで認証省略を起動できないことを検証する。
- Stripeはテスト鍵だけを使用し、Webhook署名を検証する。戻りURLだけで注文を成功にしない。イベント重複、逆順、再試行、引当期限、遅延成功を処理する。
- SMTP認証情報、Hook秘密、Stripe鍵、Supabaseサービスロール鍵、Googleクライアント秘密をGit、画面、ログへ出さない。秘密は環境別に管理し、SMTPの管理画面には平文を再表示しない。
- SQL変更は必ず`supabase/migrations/`へ追加し、RLSポリシーSQLと許可・拒否テストを同じチケットまたは依存チケットに含める。リモートDBをダッシュボードで直接変更して履歴をずらさない。Productionを`db reset`しない。
- 住所・注文・会員情報は設計の30日削除方針を守る。Stripeへ個人情報を渡さず、削除と監査で秘密・住所本文を残さない。

## 5. 金額・在庫・UI

税込円の整数で計算する。通常送料は全国一律940円、税込商品代金10,000円以上で通常送料0円。20kg以上の商品は商品ごとにヤマト運輸公式運賃を加算し、離島という理由だけでは加算しない。税率10%、税率ごとに注文単位で内税額を一回切り捨てる。注文時の価格・住所・商品・送料規則をスナップショットで保存する。カート送料は概算、注文確認は正式額。決済中だけ引当し、失敗・期限切れで解放する。

色は主背景`#F7F9FC`、サーフェス`#FFFFFF`、本文`#172033`、主操作`#155EEF`、注意`#9A5B00`、危険`#B42318`、成功`#067647`を基準とする。スマートフォン0–767px、タブレット768–1199px、PC1200px以上。320px・200%拡大、キーボード操作、WCAG 2.2 AA、44px以上の操作領域を検証する。互換性不一致でもカート・購入を許し、警告と適合商品リンクを示す。繰り返しモーダルを出さない。異常系は原因と次の操作を表示する。

## 6. テストと外部作業の停止条件

各チケットに記載されたVitest、DB統合、RLS、Playwright、手動確認を実行する。特に在庫競合、金額境界、Webhook重複・失敗、他人データ拒否、レスポンシブを省略しない。CI失敗を無視して先へ進めない。

GitHub、Vercel、Supabase、Google、Stripe、SMTP、SMS等で利用者によるアカウント作成・接続・認証・有料契約が必要なときは、必要な操作、保存する情報、費用の有無を具体的に提示する。本人に代わって契約・課金・認証しない。設計範囲のローカル作業は進めてよいが、外部設定が必要な受け入れ条件を未確認のまま完了にしない。
